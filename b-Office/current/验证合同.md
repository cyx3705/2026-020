# HistoryJanus 验证合同

## 5.11.0 验证（REQ-027）

- `LfsPolicyContractTests`（新增，纯函数）：精确路径带前导 `/`；空格、引号、通配字符往返不丢；
  oversize 块永远在 `.gitignore` 最后且不重复；lfs 块改写幂等、原位；托管块外 LFS 行全部计为违规，
  去掉后保留 `-text`；「极保守」旧说明被替换；只有顶层 `z-*` 算快照。
- `LfsRulesSuite`（新增冒烟，真实 git + git-lfs）：
  - 扩展名通配把 1KB 文件存成指针的仓，预演不改任何东西；修复后该文件以真实内容入库、
    `*.bin -text` 保留、HEAD 只多一次提交、通过提交前门。
  - 托管块外有 `filter=lfs` 时 `CommitAsync` 返回失败，消息点名 `janus.gitrule.lfsrepair`。
  - 不到 100MB 的文件定 LFS 被拒；`z-*` 文件定「不纳入 git」被拒；≥100MB 文件定 LFS 后下次提交是指针；
    改为「不纳入 git」后 oversize 块在文件末尾，下次提交移出索引、本地文件还在。
- `ProjectOperationsPanelContractTests`：四个子页标题与 case 逐字相等；入库规则不带项目名；
  LFS 页两张表的 id 与取数参数；三个行操作绑定已声明动作且参数取行内 `name`/`path`。
- `CommandCatalogContractTests` 44 条业务命令、`lfsrepair` 需确认；ModuleSmoke 与质量门 54 条运行时命令。

## 5.10.0 验证（REQ-019 / REQ-020 / REQ-021）

- `RemoteBranchVisibilityContractTests`（新增）：
  - `RefreshFetchesEveryRemoteHeadNotOnlyMainline` 钉住 `ProjectService.AllHeadsRefspec`
    的字面值，退回单分支写法立刻红。
  - `ARefreshBringsSomeoneElsesBranchHomeAndTheGraphDrawsIt` 是端到端那一条：
    本机裸仓当远端，另一份克隆推 `feature/bridge-guard`；刷新前本地没有这条跟踪引用，
    刷新后有了，`janus.graph.branches` 把它画成开着的平行泳道，
    继承关系的父分支是项目本身、基线非空。分支名刻意不带 `ai/` 前缀——正是 5.9 会丢掉的那类。
  - `TheRemoteHeadSymrefDoesNotBecomeItsOwnLane`：`origin/HEAD` 不得变成一条泳道。
  - 三条都只用本机裸仓，不碰外网。
- `RemoteBranchSyncContractTests`（新增，REQ-022）：
  - `AProjectWithUnbuiltRemoteBranchesIsNotReportedAsReadyToArchive`：main 一致但远端有
    未建分支时状态是「同步」且消息说得出差几条；同步后本地真的多出那条分支、
    上游指向 `origin/<名字>`；再刷新才回到「归档」。
  - `BehindBranchesFastForwardWhileDivergedOnesAreLeftAlone`：落后的快进到远端 SHA，
    已分叉的一个字节都没动，回执里说得出跳过的条数与原因。分叉用 `commit-tree` +
    `update-ref` 造，**不碰 HEAD**——在 main 上补提交会让项目整体变「领先」，
    那时同步本来就会被拒，测到的就不是这件事了。
  - `ASingleBranchRemoteSyncsWithoutMentioningBranches`：远端只有 main 时回执不提分支。
  - `TheRemoteHeadSymrefIsNotMaterialisedAsABranch`：`origin/HEAD` 不落地。
- `GitPathDisplayContractTests`（新增）：
  - `GitPrintsChinesePathsAsCharactersNotOctalEscapes` 在真仓上查 `diff --stat`（已跟踪）
    与 `status --porcelain`（未跟踪）两条路——弹窗正文两段分别来自它们——
    断言出现的是中文字面而**不含** `\345` / `\346` 这样的八进制前缀。
  - `QuotedPathsAreRestoredAndPlainOnesAreLeftAlone` 覆盖 `GitPath.Unquote` 的八种形状：
    普通路径、含空格、含引号、反斜杠结尾、八进制转义、控制字符，以及不成对引号与空串原样返回。
- 2026-09-19 实测：Release 构建 0 警告 0 错误；`Contracts` **110/110 通过**（94 → 110）。
- 2026-09-19 实测（真机，非测试桩）：`2026-028-LocalChat` 的远端有
  `feature/bridge-confirmation-guard`，改前本地 `git branch -a` 只有 `origin/main`；
  按新 refspec 取一次后该跟踪引用落地。全库 47 个项目的 fetch 计时：
  并发 6 为 31.6 秒、16 为 12.0 秒（新默认）、24 为 8.4 秒、47 为 5.1 秒，全程无失败。
- **未运行、剩余风险**：
  - 「同步落地分支」没有在**带 AI 工作区**的真实项目上跑过。「被工作树签出就跳过」
    这一条只有合同测试覆盖（测试仓没有附加工作树），真机行为待验收。
  - 并发 16 的实测是在链路正常的那一段做的。本机 GitHub SSH 会间歇性整段不通
    （见 [[machine-github-ssh-blocked]]），那种时段下 16 条并发一起超时的表现未实测；
    单条失败仍按既有路径退化为「刷新 ?」，不影响其它项目。
  - 泳道放宽之后，杂分支多的仓图谱会变宽。库内当前各仓远端分支极少（多数只有 main），
    因此「泳道太多以致读不了」这一情形没有真机样本。
  - 提交差异弹窗的中文文件名有真仓命令级实测，但弹窗本身的真机点击仍待验收
    （与 5.9.0 遗留的同一项）。

## 5.9.0 验证（REQ-014 / REQ-016 / REQ-017 / REQ-018）

- `CommandCatalogContractTests`：命令总数 39 → **42**（新增 `janus.proj.diff`、
  `janus.proj.discard`、`janus.gitrule.lfs`）；`diff` 与 `gitrule.lfs` 进只读投影，
  `discard` 进确认闸口清单。
- `ProjectOperationsPanelContractTests.EnteringASectionRefetchesItInsteadOfWaitingForARefreshButton`：
  子页面选项框的 `commitAction` 指向 `janus.section.enter`，动作转 `janus.ui.sectionenter`
  并带 `{section}`；项目操作页上不再有任何「刷新 X」按钮，两条旧动作声明也已删除。
- `ProjectOperationsPanelContractTests.TheRuleTableIsScopedToTheSelectedProjectForItsLfsRows`
  与 `EveryGitBackedViewIsScopedToTheSelectedProject`：`rule-list` 取数带
  `{selection.janus.project.name}`，与分支历史、图谱同一条约束。
- 2026-09-18 实测：Debug 构建 0 警告 0 错误；`Contracts` **85/85 通过**；
  `Test-QualityGate.ps1` 通过。
- `ArchiveTransportContractTests`（2026-09-19 新增）：SSH→HTTPS 地址换算的正反例
  （含 Windows 本地路径不得换算），以及 SSH 连不上时确实换 HTTPS 再试、
  进度里说得出「改用 HTTPS 重试」、两条路都不通时失败消息两条都有交代。
  两端都指向本机死端口，不碰外网。
- 2026-09-19 实测：Release 构建 0 警告 0 错误；`Contracts` **94/94 通过**；
  `Test-QualityGate.ps1` 通过（从 AI 工作树运行需传 `-HistoryVulcanPackageRoot`）。
- **未运行、剩余风险**（本轮不拿真实项目做破坏性验证）：
  - `janus.proj.discard` 没有在真实脏工作树上跑过。它不可撤销，因此只做到确认闸口与
    命令目录合同；实际删除行为待用户在一个可丢弃的项目上验收。
  - ~~`ArchiveCloner` 的分段加深没有在真实大仓上跑通过~~：2026-09-19 01:31 已实测。
    `janus.proj.pull name=2025-001-AGV洗轮机` 成功，445MiB 一次收完、1136 个文件签出、
    LFS 单独取回无补充说明，落地 1.3GB、22 个提交、非浅（因此加深一步按设计直接跳过）。
    **走的是 SSH**——那一刻链路已恢复，因此这次证明的是分段与进度这条路，
    HTTPS 回退在本次真机拉取中没有触发，只有合同测试覆盖。
  - 提交差异弹窗未经真机点击；它依赖 Aurora 1.24.0 的 content + options，
    该能力在 Aurora 侧有合同测试，但两仓合起来的真机效果待验收。

## 5.7.0 验证

Contracts 81 项通过，包括四列投影、状态单元格绑定原生命周期动作与参数合同。
Debug/Release 全部九组功能 Smoke 通过；ModuleSmoke 兼容管线传入的 bin：在临时运行区构造带校验和的模块包，验证装载、48 条命令、三个页面、重载与卸载。已有校验和的完整候选不重算哈希，仍接受正式发现器校验。
第一次 submit 因 ModuleSmoke 的输入合同与管线不一致而失败，现已修正并通过定向装载冒烟。
关联前端验收由 Aurora 工作区执行：规则表窄/宽布局均可滚动，按钮运行态与防重复触发、异常恢复。
正式收尾仍待 submit 热重载后的用户验收；本轮不运行任何真实项目提交、推送或归档动作。

## 5.6.0 总览筛选与分支历史专项

- `OverviewFilterContractTests`：最新优先的行序；搜索同时命中项目名与 z 级文件夹名；
  年份过滤与「全部」短路；两者求交；年份候选首项为「全部」且其余倒序；
  没有年份前缀的项目不被过滤掉也不产生候选。
- `BranchHistoryRowContractTests`：HEAD 是第一行、分叉点是最后一行；
  列去掉之后行里仍带 `sha` 与 `author`；非报文载荷返回空行集。
- `ProjectOperationsPanelContractTests.OverviewUsesTheV56ControlPanelAndClickableColumns`：
  页签标题为 `Janus`；顶栏三件套的通道名与 `optionsSource`；表格取数参数里
  **没有** `refresh`（否则每敲一个字符都会 fetch 全部远端）。
- 2026-09-07 实测：Debug 与 Release 构建均 0 警告 0 错误；`Contracts` 81、九套 `Smoke` 全绿；
  `ModuleSmoke` 对候选包 PASS（48 条命令 / 三个页面）；`Test-QualityGate.ps1` 通过。
- `ModuleSmoke` 随宿主快照对齐（与 5.6.0 的页面改动无关，但它挡着整解构建）：
  宿主 5.1.x 只经 `IModuleDiscoverySource` 发现模块，`EnableCommands` 与 `ChangeDirectory`
  都已不存在。参数因此从「模块 bin 目录」改成**运行区根目录**，跑 `RuntimeModuleDiscoverySource`
  扫候选包；卸载检查改用 `ModuleHost.Unload`。命令：
  `dotnet run --project .\b-Code-Verify\ModuleSmoke\ModuleSmoke.csproj -c Release -- .\z-Publish`。

## 5.5.1 项目总览专项

- `ProjectLifecycleContractTests` 使用真实临时裸远端覆盖无 origin、ahead、behind、diverged、未验证一致、同步、多个 z/Z 归档和拉取覆盖。
- `UiProjectProjectionContractTests` 锁定 0/1/多个 z/Z 文件夹显示，以及「动作 + 符号」投影：提交 `●`、推送 `↑`、同步 `↔`、归档 `□`、拉取 `↓`，未知动作回退为「刷新 `?`」（5.8.1）。
- `ProjectLifecycleContractTests.UnreachableRemoteAsksForRefreshInsteadOfSync` 锁定远端不可达时动作为「刷新」，且按已 fetch 快照回写不会被陈旧引用算回「同步」（5.8.1）。
- `ProjectOperationsPanelContractTests` 锁定标题删除、刷新图标、四列（第三列「操作」）、项目名 / z 文件夹 / 操作三个 `cellAction`（项目名 → `janus.proj.open`）、状态普通文本，以及第三行“提交/推送”。
- 归档历史只走 GitHub API 与现有 GCM 凭据；认证失败、非 GitHub origin 和网络错误必须明确报告。
- 运行时命令应为 48 条：39 条业务命令、8 条 Aurora UI 命令与 `janus.status`。

## 验证原则

- 日常开发只运行相关 Contracts、Debug 构建、定向功能 Smoke 和 ModuleSmoke。
- 正式发布运行 Debug/Release 构建、Contracts、完整功能 Smoke、ModuleSmoke、包静态门禁、checksum、双槽部署和回滚验证。
- 功能 Smoke 当前固定为 `VersionProjection`、`TestArchitecture`、`GitRules`、`BranchHistory`、`BranchGraph`、`SubmoduleSafety`、`RepositoryTargets`、`ProjectOperations` 和 `GitHub` 九套；宿主装载、卸载和热重载只由独立 ModuleSmoke 验证。
- Architecture/Contracts 将覆盖 AI 工作树状态机与 HEAD 失效规则（DEC-012），即使 4.0.0 尚未注册 `janus.aiworktree.*`。
- 正式流程不得运行或修改开机自启动，也不得替用户启动 HistoryVulcan。
- 运行时命令目录、窗口注册、module manifest 和正式包文件集合是可执行事实；文档必须与其一致。
- 产品 Git 进程不设应用级墙钟超时；宿主取消仍必须终止完整进程树。GitHub 诊断和 Smoke
  的具名超时是独立安全边界，不得随产品 Git 超时一起删除。
- 排除清单刷入 `.gitignore` 必须可重复执行且字节一致，块外手写内容逐字保留；
  提交链路必须在不经任何人工下发的情况下让清单生效。规则页写入只允许经
  `janus.gitrule.excludes` 走宿主确认，空清单不得发出命令。
- 六条退役的 `gitrule` 命令必须无别名残留；页面不得再出现 DataGrid 或格式输入行。
- 项目总览的工作树状态必须覆盖干净、已跟踪修改、未跟踪文件和恢复干净；状态读取失败显示未知，
  不得阻断其余工作树。
- 图谱布局必须用倒序跨分支时间戳、已合并多提交越界和开放行时间跨度验证父边赋 X：开放分支首节点
  紧邻分叉点右侧，已合并历史分支同时贴住分叉与 merge；只验证节点仍在独立泳道不足以关闭该回归。
- 首次推送建远端仓库必须在门禁内**离线**验证：GitHub Smoke 用假 provisioner 和假 HTTP handler 覆盖
  建仓并推送、422 复用、建仓失败不留半配 origin、已有 origin 不触碰 provisioner 四条路径。
  门禁不得发起真实 GitHub 请求，也不得读取真实 GCM 凭据。
- 页面不得自建 `Window` 或调用 `MessageBox.Show`；预览/提示/通知必须编码为 `aurora.ui.dialog`。
  写操作确认只允许命令 `ConfirmPrompt`，不得在页面再叠一层。
- `Test-QualityGate.ps1` 必须阻断根 `project.manifest.json` / `AGENTS.md` 缺失、项目身份与版本漂移，
  以及模块 API 的版本、宿主基线、窗口清单、命令总数或 z 快照副本漂移。
- 三块子内容（Git 文件规则 / 分支历史 / GitHub）**不再是页面**：5.4.6 起收进 `projops` 的
  `switch` 容器，由控制面板里的轮换选项框切换（5.4.5 曾是三个 `side=bottom` 页面组成的标签组）。
  仍然不得为它们引入 `toggleGroup`——那是 Aurora 的缺件，写了只出占位牌。
  选项框的候选项与三支的 `case` 必须逐字相等：对不上时不报任何错，
  表现是切过去之后下面永远停在第一支。
- 会跑 Git 的取数（落地状态、分支历史、图谱）必须按单个项目取，项目名来自选择通道；
  缺项目名时拒绝取数，不得退化成扫全库。
- 页面描述里不得出现已退役的页面节点（`button` / `input` / `select`）。它们不报错，
  只是渲染成一块写着原因的牌子——而牌子是能用的界面的反面，肉眼看得见但门禁看不见，
  所以必须按**描述文本**判死。
- 「项目操作」面板与项目表的接线要逐条核对：通道名、跟随的列、四个按钮的启停条件，
  以及每个动作参数取自选中行还是取自输入框。这条链路的两端分属两个仓，
  写错一个字的症状是「按钮永远灰着」或「改名把项目改成了它自己」——都不报错。
- 面板声明里不得出现 `required`。面板的必填校验是全局的，一个空必填框会锁死面板上
  每一个按钮，而报出来的错说的是那个框的名字，看上去与被锁死的按钮毫无关系。

## 验证层级

| 编号 | 层级 | 适用时机 | 命令或步骤 | 通过标准 |
| --- | --- | --- | --- | --- |
| VERIFY-FAST | 快速检查 | 相关功能完成 | `dotnet build HistoryJanus.sln -c Debug`；定向 Contracts/Smoke；ModuleSmoke；`b-Code-Studio/eng/Test-QualityGate.ps1` | 0 warning、0 error，选定测试全绿；API 与版本源一致、页面 3 个、运行时命令 48 条；生命周期、归档/拉取、项目改名与路径安全门禁通过；产品 Git 无自决超时；分支节点按父边赋 X |
| VERIFY-CI | 推送门禁 | 推送到 `2026-020-HistoryJanus` | `.github/workflows/historyjanus-gate.yml`：锁定还原、双配置构建、Contracts、`dotnet format` 验证、质量门禁 | 流水线全绿 |
| VERIFY-FULL | 完整回归 | 正式候选 | 宿主 `vulcan.dev.submit name=HistoryJanus`：构建、合同、Smoke、写入版本化 z 并热重载 | 宿主回执成功；Janus 的构建、Contracts、功能 Smoke、ModuleSmoke、包静态检查和文档对齐项全部通过 |
| VERIFY-PUBLISH | 正式提升 | 用户显式批准 | 宿主 `vulcan.dev.finish name=HistoryJanus`：工作区干净后并回主树并安装主树候选 | `z-Publish/HistoryJanus-vX.Y.Z/`（含 docs）来自合并后的工作区候选；失败不隐式提交或推送 |
| VERIFY-ACCEPT | 人工验收 | 用户重启 HistoryVulcan 后 | 模块管理、三个页面、项目与 Meta 列举、独立分支图谱、Git 文件规则、内嵌分支历史与 GitHub 的同级切换 | 模块可见，页面功能生效，UI 与后台命令来源一致；真实已合并分支同时贴住分叉与 merge，不越过分叉点，也不回到远处共同祖先 |

## 5.4.6 项目操作页专项

| 场景 | 判据 | 用例 |
| --- | --- | --- |
| 三块收进一页 | 整份描述只剩 `overview` / `graph` / `projops`；选项框的候选项与 `switch` 三支的 `case` **逐字相等** | `TheThreeSectionsAreFoldedIntoTheOperationsPageBehindOneOptionBox` |
| 没有页首说明段落 | 页面树里的 `text` 节点一律不超过 12 字 | `NoPageStillCarriesAPreambleParagraph` |
| 刷新按钮按节点刷 | `janus.github.refresh` 绑 `node=github-rows` 且不带 `page=`；`janus.rules.refresh` 绑 `janus.ui.refreshrules`；两个按钮都在 `projops` 上 | `TheRefreshButtonsActuallyRefetchTheirOwnNodes` |

「候选项与 `case` 逐字相等」这一半是必须的：两边对不上时**不报任何错**，
界面上的表现是切到某一项后下面永远停在第一支——与「这一支没数据」长得一样。

**依赖 Aurora ≥ 1.8.17。** 低于该版本时选项框的 `channel` 被当作未知字段忽略，
`switch` 渲染成一块「该组件待交付」的占位牌。

## 5.4.5 项目操作面板专项

| 场景 | 判据 | 用例 |
| --- | --- | --- |
| 项目表发布选中行 | `overview` 的表格声明 `channel = janus.project`，且列里真的有 `name` | `TheProjectTablePublishesItsSelectionOnTheProjectChannel` |
| 四个声明行 | 前三行各一个文本框（`project-name` / `new-project` / `commit-message`）且都写 `flex`，第四行是 `even` 的 `section`；四个按钮文案依次为 改名 / 新建 / 提交当前项目 / 推送当前项目 | `TheOperationsPanelIsFourDeclaredRows` |
| 只有项目名跟随选中 | 声明 `follows` 的只有 `project-name`，跟的是 `janus.project.name` | `OnlyTheProjectNameBoxFollowsTheSelection` |
| 四个按钮都按选中启停 | 每个按钮的 `enabledWhen.selected` 都是 `janus.project` | `EveryButtonIsGatedOnHavingASelectedProject` |
| 不用 `required` | 面板里没有任何 `required` 字段（Aurora 1.9.2 起它已退役，写了只会被静默忽略） | `NoWidgetIsMarkedRequiredBecauseThatFieldIsRetired` |
| 改名取两处 | `name` 取 `{selection.janus.project.name}`，`new` 取 `{project-name}` | `RenameTakesTheTargetFromTheSelectionAndTheNewNameFromTheBox` |
| 新建以选中为模板 | `name` 取 `{new-project}`，`base` 取选中行 | `CreateInheritsFromTheSelectedProject` |
| 提交与推送作用于选中 | 两条的 `name` 都取选中行；提交的 `msg` 取 `{commit-message}` | `CommitAndPushActOnTheSelectedProject` |
| 兜底自洽 | 按钮引用的动作都已声明；动作里的占位符要么是声明过的通道，要么是本面板真有的控件 id | `EveryButtonResolvesAndEveryPlaceholderPointsAtSomethingReal` |
| 无退役节点 | 描述文本里不含 `"type":"button"` / `"input"` / `"select"` | `NoPageStillUsesARetiredNodeType` |

**依赖 Aurora ≥ 1.9.2**（面板的行是一等结构，REQ-UI-060）——这是硬依赖：
更早的 Aurora 只认 `widgets`，拿到只写 `rows` 的声明会画出一块空面板。
更早的下限备查：低于 1.8.16 时 `channel` / `follows` / `enabledWhen`
会被当作未知字段忽略，表现为按钮常亮、跟随框不填——**不报错**，因此这一条只能靠人工验收看。

### 三块子内容（5.4.5 起，5.4.6 改为收进 `projops`）

| 场景 | 判据 | 用例 |
| --- | --- | --- |
| 会跑 Git 的取数按单项目 | `history` / `graph` 的 `name` 都取自选择通道（`rulestate` 已于 5.8.1 删除） | `EveryGitBackedViewIsScopedToTheSelectedProject` |
| 清单不碰 Git | `excludes` 视图不带 `name`，开页即出 | `TheSharedExcludeListLoadsWithoutTouchingGit` |

**本轮未做**：真机目视。上面各条都是对声明文本的核对，证明的是「本仓这份声明前后一致」，
不是「装上去真的能点」。后者要 Aurora 1.8.17 装好之后由人验收——
尤其是轮换选项框转一下、下面那块真的跟着换。

## 图谱布局专项

| 场景 | 夹具 | 通过标准 |
| --- | --- | --- |
| 开放分支时钟早于分叉点 | 主线 `root -> fork`，分支提交的时间早于 `fork`，但父提交是 `fork` | 分支首节点横坐标 = `fork` 右侧一个标准节点间距 |
| 已合并分支仍共享主线祖先 | 主线 `root -> main-before -> merge`，分支 `root -> base -> tip`，`merge` 第二父为 `tip` | 历史泳道末节点横坐标 = `merge` 左侧一个标准节点间距，不得优先锚定 `root` |
| 已合并多提交长于分叉到 merge | 主线 `fork -> mid -> merge`，分支 `fork -> b1..b4`，`merge` 第二父为 `b4`，分支时间戳全部早于主线 | 首节点紧邻 fork 右侧，末节点紧邻 merge 左侧；不得因整行平移而越过分叉点 |
| 开放行时间跨度大于主线 | 主线 `fork -> tip`，开放分支三提交时间戳跨数月 | 相邻分支节点间距为一个标准节点间距，行宽由提交数决定，不随时间跨度拉长 |
| 已删除已合并 ref | 删除分支 ref，仅由 merge 第二父恢复历史泳道 | 泳道仍存在、无开放 tip，且继续使用 merge 锚点 |
| 不相交已合并行 | 主线 `root -> a -> m1 -> b -> m2`，左支 `root -> left` 合入 `m1`，右支 `b -> right` 合入 `m2` | 两支都在主线下一行，不得把左支放到更低的空行 |
| 相交平行行 | 两条支线从同一分叉点出发且水平跨度重叠 | 先占用者留在主线下一行，后者下移一行 |

## 环境前提

- Windows 与 .NET 8 SDK。
- HistoryVulcan current-host 快照位于 `C:/OneHistory/HistoryClio/2026-023-HistoryVulcan/z-Publish`；版本和 `sourceDirty` 以其 manifest 为准。
- 正式发布的源码洁净度、候选证据、促级顺序和失败回滚以宿主 `vulcan.dev.submit` / `finish` 为准；
  Janus 文档不复制一套可能漂移的外部管线参数。Diana 不发布。
- 模块部署不要关闭 HistoryVulcan。管线在宿主运行时原地覆盖 z 并热重载；只有替换宿主 EXE 才停进程。

## 证据保存

| 证据类型 | 保存位置 | 是否入库 | 保留规则 |
| --- | --- | --- | --- |
| 最新正式模块及校验 | `z-Publish/HistoryJanus-vX.Y.Z/` | 是 | 只保留最新一个版本化候选 |
| 被替换的正式包 | `z-Publish/history/HistoryJanus-v*` | 是 | 只保存历史正式包，不保存构建工作区 |
| 当前候选 | `z-Publish/HistoryJanus-vX.Y.Z/` | 是 | 下一次候选原子覆盖同名目录 |
| 部署回滚 | HistoryVulcan Modules 同级 `HistoryJanus-rollback-*` | 否 | 至少保留本次部署前版本 |

## 明确不执行

- 开机自启动测试。
- 自动启动 HistoryVulcan 或旧 Janus。
- 真实 GitHub 凭据修改、真实推送、UAC、防火墙和双机 LAN 测试。


## VERIFY-SCENE-REG

已通过：Janus 三页均声明 `scene=HistoryJanus`；Aurora 热重载后当前 Janus 场景仍为总览中间、图谱底部、项目操作左侧且均可见，其他场景布局未被改写。
