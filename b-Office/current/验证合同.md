# HistoryJanus 验证合同

## 验证原则

- 日常开发只运行相关 Contracts、Debug 构建、定向功能 Smoke 和 ModuleSmoke。
- 正式发布运行 Debug/Release 构建、Contracts、完整功能 Smoke、ModuleSmoke、包静态门禁、checksum、双槽部署和回滚验证。
- 功能 Smoke 当前固定为 `VersionProjection`、`TestArchitecture`、`GitRules`、`BranchHistory`、`BranchGraph`、`SubmoduleSafety`、`RepositoryTargets`、`ProjectOperations` 和 `GitHub` 九套；宿主装载、卸载和热重载只由独立 ModuleSmoke 验证。
- Architecture/Contracts 将覆盖 AI 工作树状态机与 HEAD 失效规则（DEC-012），即使 4.0.0 尚未注册 `janus.aiworktree.*`。
- 正式流程不得运行或修改开机自启动，也不得替用户启动 HistoryVulcan。
- 运行时命令目录、窗口注册、module manifest 和正式包文件集合是可执行事实；文档必须与其一致。
- 产品 Git 进程不设应用级墙钟超时；宿主取消仍必须终止完整进程树。GitHub 诊断和 Smoke
  的具名超时是独立安全边界，不得随产品 Git 超时一起删除。
- 排除清单刷入 `.gitignore` 必须可重复执行且字节一致，块外手写内容逐字保留；
  提交链路必须在不经任何人工下发的情况下让清单生效。规则页写入只允许经
  `janus.gitrule.excludes` 走宿主确认，空清单不得发出命令。
- 六条退役的 `gitrule` 命令必须无别名残留；页面不得再出现 DataGrid 或格式输入行。
- 项目总览的工作树状态必须覆盖干净、已跟踪修改、未跟踪文件和恢复干净；状态读取失败显示未知，
  不得阻断其余工作树。
- 图谱布局必须用倒序跨分支时间戳、已合并多提交越界和开放行时间跨度验证父边赋 X：开放分支首节点
  紧邻分叉点右侧，已合并历史分支同时贴住分叉与 merge；只验证节点仍在独立泳道不足以关闭该回归。
- 首次推送建远端仓库必须在门禁内**离线**验证：GitHub Smoke 用假 provisioner 和假 HTTP handler 覆盖
  建仓并推送、422 复用、建仓失败不留半配 origin、已有 origin 不触碰 provisioner 四条路径。
  门禁不得发起真实 GitHub 请求，也不得读取真实 GCM 凭据。
- 页面不得自建 `Window` 或调用 `MessageBox.Show`；预览/提示/通知必须编码为 `aurora.ui.dialog`。
  写操作确认只允许命令 `ConfirmPrompt`，不得在页面再叠一层。
- `Test-QualityGate.ps1` 必须阻断根 `project.manifest.json` / `AGENTS.md` 缺失、项目身份与版本漂移，
  以及模块 API 的版本、宿主基线、窗口清单、命令总数或 z 快照副本漂移。
- 三块子内容（Git 文件规则 / 分支历史 / GitHub）**不再是页面**：5.4.6 起收进 `projops` 的
  `switch` 容器，由控制面板里的轮换选项框切换（5.4.5 曾是三个 `side=bottom` 页面组成的标签组）。
  仍然不得为它们引入 `toggleGroup`——那是 Aurora 的缺件，写了只出占位牌。
  选项框的候选项与三支的 `case` 必须逐字相等：对不上时不报任何错，
  表现是切过去之后下面永远停在第一支。
- 会跑 Git 的取数（落地状态、分支历史、图谱）必须按单个项目取，项目名来自选择通道；
  缺项目名时拒绝取数，不得退化成扫全库。
- 页面描述里不得出现已退役的页面节点（`button` / `input` / `select`）。它们不报错，
  只是渲染成一块写着原因的牌子——而牌子是能用的界面的反面，肉眼看得见但门禁看不见，
  所以必须按**描述文本**判死。
- 「项目操作」面板与项目表的接线要逐条核对：通道名、跟随的列、四个按钮的启停条件，
  以及每个动作参数取自选中行还是取自输入框。这条链路的两端分属两个仓，
  写错一个字的症状是「按钮永远灰着」或「改名把项目改成了它自己」——都不报错。
- 面板声明里不得出现 `required`。面板的必填校验是全局的，一个空必填框会锁死面板上
  每一个按钮，而报出来的错说的是那个框的名字，看上去与被锁死的按钮毫无关系。

## 验证层级

| 编号 | 层级 | 适用时机 | 命令或步骤 | 通过标准 |
| --- | --- | --- | --- | --- |
| VERIFY-FAST | 快速检查 | 相关功能完成 | `dotnet build HistoryJanus.sln -c Debug`；定向 Contracts/Smoke；ModuleSmoke；`b-Code-Studio/eng/Test-QualityGate.ps1` | 0 warning、0 error，选定测试全绿；API 与版本源一致、页面 3 个、运行时命令 41 条；项目改名安全门禁通过；产品 Git 无自决超时；按名解析单仓不扫全库；排除清单幂等落地，总览工作树状态三态正确；分支节点按父边赋 X，多提交行不得越过分叉点 |
| VERIFY-CI | 推送门禁 | 推送到 `2026-020-HistoryJanus` | `.github/workflows/historyjanus-gate.yml`：锁定还原、双配置构建、Contracts、`dotnet format` 验证、质量门禁 | 流水线全绿 |
| VERIFY-FULL | 完整回归 | 正式候选 | 运行 Diana 正式 `moduleDevelopment` 合同登记的 HistoryJanus 候选门禁；具体工具名和参数不在 Janus 内重复冻结 | Diana 返回成功证据；Janus 的构建、Contracts、功能 Smoke、ModuleSmoke、包静态检查和文档对齐项全部通过 |
| VERIFY-PUBLISH | 正式提升 | 用户显式批准 | 按同一 Diana 合同从主树执行正式提升，工作树候选不得直接促级 | z 级正式树（含 docs）原子更新；失败按 Diana 合同回滚，源码提交、推送不由管线隐式执行 |
| VERIFY-ACCEPT | 人工验收 | 用户重启 HistoryVulcan 后 | 模块管理、三个页面、项目与 Meta 列举、独立分支图谱、Git 文件规则、内嵌分支历史与 GitHub 的同级切换 | 模块可见，页面功能生效，UI 与后台命令来源一致；真实已合并分支同时贴住分叉与 merge，不越过分叉点，也不回到远处共同祖先 |

## 5.4.6 项目操作页专项

| 场景 | 判据 | 用例 |
| --- | --- | --- |
| 三块收进一页 | 整份描述只剩 `overview` / `graph` / `projops`；选项框的候选项与 `switch` 三支的 `case` **逐字相等** | `TheThreeSectionsAreFoldedIntoTheOperationsPageBehindOneOptionBox` |
| 没有页首说明段落 | 页面树里的 `text` 节点一律不超过 12 字 | `NoPageStillCarriesAPreambleParagraph` |
| 刷新按钮按节点刷 | `janus.github.refresh` 绑 `node=github-rows` 且不带 `page=`；`janus.rules.refresh` 绑 `janus.ui.refreshrules`；两个按钮都在 `projops` 上 | `TheRefreshButtonsActuallyRefetchTheirOwnNodes` |

「候选项与 `case` 逐字相等」这一半是必须的：两边对不上时**不报任何错**，
界面上的表现是切到某一项后下面永远停在第一支——与「这一支没数据」长得一样。

**依赖 Aurora ≥ 1.8.17。** 低于该版本时选项框的 `channel` 被当作未知字段忽略，
`switch` 渲染成一块「该组件待交付」的占位牌。

## 5.4.5 项目操作面板专项

| 场景 | 判据 | 用例 |
| --- | --- | --- |
| 项目表发布选中行 | `overview` 的表格声明 `channel = janus.project`，且列里真的有 `name` | `TheProjectTablePublishesItsSelectionOnTheProjectChannel` |
| 四行「左标签 / 中控件 / 右按钮」 | 文本框按序为 `project-name` / `new-project` / `commit-message` / `section`（第四行 5.4.6 加入）；四个按钮全部 `inline`，文案依次为 改名 / 新建 / 提交当前项目 / 推送当前项目 | `TheOperationsPanelIsFourRowsOfLabelControlAndButtons` |
| 只有项目名跟随选中 | 声明 `follows` 的只有 `project-name`，跟的是 `janus.project.name` | `OnlyTheProjectNameBoxFollowsTheSelection` |
| 四个按钮都按选中启停 | 每个按钮的 `enabledWhen.selected` 都是 `janus.project` | `EveryButtonIsGatedOnHavingASelectedProject` |
| 不用 `required` | 面板里没有任何 `required: true` | `NoWidgetIsMarkedRequiredBecauseThatValidationIsPanelWide` |
| 改名取两处 | `name` 取 `{selection.janus.project.name}`，`new` 取 `{project-name}` | `RenameTakesTheTargetFromTheSelectionAndTheNewNameFromTheBox` |
| 新建以选中为模板 | `name` 取 `{new-project}`，`base` 取选中行 | `CreateInheritsFromTheSelectedProject` |
| 提交与推送作用于选中 | 两条的 `name` 都取选中行；提交的 `msg` 取 `{commit-message}` | `CommitAndPushActOnTheSelectedProject` |
| 兜底自洽 | 按钮引用的动作都已声明；动作里的占位符要么是声明过的通道，要么是本面板真有的控件 id | `EveryButtonResolvesAndEveryPlaceholderPointsAtSomethingReal` |
| 无退役节点 | 描述文本里不含 `"type":"button"` / `"input"` / `"select"` | `NoPageStillUsesARetiredNodeType` |

**依赖 Aurora ≥ 1.8.16。** 低于该版本时 `channel` / `follows` / `enabledWhen` / `inline`
会被当作未知字段忽略，表现为按钮常亮、跟随框不填——**不报错**，因此这一条只能靠人工验收看。

### 三块子内容（5.4.5 起，5.4.6 改为收进 `projops`）

| 场景 | 判据 | 用例 |
| --- | --- | --- |
| 会跑 Git 的取数按单项目 | `rulestate` / `history` / `graph` 的 `name` 都取自选择通道 | `EveryGitBackedViewIsScopedToTheSelectedProject` |
| 清单不碰 Git | `excludes` 视图不带 `name`，开页即出 | `TheSharedExcludeListLoadsWithoutTouchingGit` |

**本轮未做**：真机目视。上面各条都是对声明文本的核对，证明的是「本仓这份声明前后一致」，
不是「装上去真的能点」。后者要 Aurora 1.8.17 装好之后由人验收——
尤其是轮换选项框转一下、下面那块真的跟着换。

## 图谱布局专项

| 场景 | 夹具 | 通过标准 |
| --- | --- | --- |
| 开放分支时钟早于分叉点 | 主线 `root -> fork`，分支提交的时间早于 `fork`，但父提交是 `fork` | 分支首节点横坐标 = `fork` 右侧一个标准节点间距 |
| 已合并分支仍共享主线祖先 | 主线 `root -> main-before -> merge`，分支 `root -> base -> tip`，`merge` 第二父为 `tip` | 历史泳道末节点横坐标 = `merge` 左侧一个标准节点间距，不得优先锚定 `root` |
| 已合并多提交长于分叉到 merge | 主线 `fork -> mid -> merge`，分支 `fork -> b1..b4`，`merge` 第二父为 `b4`，分支时间戳全部早于主线 | 首节点紧邻 fork 右侧，末节点紧邻 merge 左侧；不得因整行平移而越过分叉点 |
| 开放行时间跨度大于主线 | 主线 `fork -> tip`，开放分支三提交时间戳跨数月 | 相邻分支节点间距为一个标准节点间距，行宽由提交数决定，不随时间跨度拉长 |
| 已删除已合并 ref | 删除分支 ref，仅由 merge 第二父恢复历史泳道 | 泳道仍存在、无开放 tip，且继续使用 merge 锚点 |
| 不相交已合并行 | 主线 `root -> a -> m1 -> b -> m2`，左支 `root -> left` 合入 `m1`，右支 `b -> right` 合入 `m2` | 两支都在主线下一行，不得把左支放到更低的空行 |
| 相交平行行 | 两条支线从同一分叉点出发且水平跨度重叠 | 先占用者留在主线下一行，后者下移一行 |

## 环境前提

- Windows 与 .NET 8 SDK。
- HistoryVulcan current-host 快照位于 `C:/OneHistory/HistoryClio/2026-023-HistoryVulcan/z-Publish`；版本和 `sourceDirty` 以其 manifest 为准。
- 正式发布的源码洁净度、候选证据、促级顺序和失败回滚以 Diana 正式 `moduleDevelopment` 合同为准；
  Janus 文档不复制一套可能漂移的外部管线参数。
- 模块部署不要关闭 HistoryVulcan。管线在宿主运行时原地覆盖 z 并热重载；只有替换宿主 EXE 才停进程。

## 证据保存

| 证据类型 | 保存位置 | 是否入库 | 保留规则 |
| --- | --- | --- | --- |
| 最新正式模块及校验 | `z-Publish` | 是 | 只保留最新版本 |
| 被替换的正式包 | `z-Publish/history/package-*` | 否 | 只保存历史正式包，不保存构建工作区 |
| 当前候选 | `z-Publish` | 否 | 下一次候选原子覆盖 |
| 部署回滚 | HistoryVulcan Modules 同级 `HistoryJanus-rollback-*` | 否 | 至少保留本次部署前版本 |

## 明确不执行

- 开机自启动测试。
- 自动启动 HistoryVulcan 或旧 Janus。
- 真实 GitHub 凭据修改、真实推送、UAC、防火墙和双机 LAN 测试。
